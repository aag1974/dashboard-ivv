name: 🔄 Manter Dashboard IVV Ativo

on:
  schedule:
    - cron: "*/5 * * * *"   # roda a cada 5 minutos
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: 🌐 Ping no Render (Dashboard IVV)
        run: |
          echo "⏰ Executando ping no Render..."
          curl -fsS https://dashboard-ivv.onrender.com > /dev/null || echo "⚠️ Render não respondeu"
